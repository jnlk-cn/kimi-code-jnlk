---
"@jnlk.zone/ganymede-code": minor
---

Turn the Git Sync sidebar into a read-only dashboard for ahead/behind commits and staged/unstaged line change stats. Open the Git Sync dock panel on a Git project to view it.
