---
"@moonshot-ai/kimi-code": patch
---

Remove the local Kimi server, browser Web UI, and Electron desktop app from this fork. The `kimi server` and `kimi web` commands and the TUI `/web` slash command are no longer available.
