---
"@jnlk.zone/ganymede-code": minor
---

Show Engineering-mode design specs from `docs/kimicodeboost/specs/` in the Plans panel alongside implementation plans, auto-open the panel when a spec is written, and let chat paths open that preview. Open Plans (⌘⇧L) after brainstorming writes a design doc.
