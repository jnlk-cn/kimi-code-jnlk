---
"@moonshot-ai/agent-core": minor
"@moonshot-ai/kimi-code-sdk": minor
"@jnlk.zone/ganymede-code": minor
---

Use a separate Ganymede data layout (`GANYMEDE_HOME` / `.ganymede`) and publish the desktop app as `@jnlk.zone/ganymede-code`. Kimi CLI keeps `~/.kimi-code`. Copy old data manually if you need it.
