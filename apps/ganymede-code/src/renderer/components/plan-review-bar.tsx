/**
 * @deprecated Prefer `PlanBuildControls`. Kept so `plan-review-modal` can re-export
 * a stable name; the live UI mounts `PlanBuildControls` from App / PlansPanel.
 */
export { PlanBuildControls as PlanReviewBar } from './plan-build-controls';
